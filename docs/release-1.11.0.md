# MindTree 1.11.0

1.11.0 新增跨导图节点链接，让日报、项目过程和长期知识可以形成可保存、可导航的个人工作网络。

## 新增

- 在节点资源面板搜索并链接任意工作区节点。
- 一键复制当前节点内部链接。
- 点击链接后切换导图、展开路径并定位目标。
- 目标删除后的失效状态提示。

## 安全边界

- 内部链接只保存文档与节点 ID，不复制正文或凭据。
- 沿用既有资源字段、Command、撤销和持久化，不增加文档 schema。
