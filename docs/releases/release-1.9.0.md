# MindTree 1.9.0

1.9.0 新增可逆的分支聚焦与专注模式，帮助用户在大型导图中只处理当前阶段，同时保留回到完整结构的清晰路径。

## 新增

- 选中节点后一键聚焦当前分支。
- 导图与大纲共享聚焦范围。
- 根节点到当前分支的面包屑导航。
- Esc 或“显示完整导图”快速退出。

## 安全边界

- 聚焦只是临时视图，不修改文档 schema、父子结构和节点位置。
- 关系、边界和摘要仅在两端都属于当前范围时显示。
- 搜索到范围外节点会自动恢复完整视图并定位。
