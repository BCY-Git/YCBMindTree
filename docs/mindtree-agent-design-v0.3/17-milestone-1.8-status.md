# 里程碑 1.8：可编辑大纲视图完成状态

更新时间：2026-07-15

## 已完成

- 顶部按钮在导图与大纲之间切换，并记住本机视图偏好。
- 大纲按 `childIds` 顺序展示完整层级，尊重原导图折叠状态。
- 自由主题不会从大纲中消失，以独立主题明确标识。
- 直接编辑节点标题，所有变化通过统一 Command 层写入，可撤销并自动持久化。
- 折叠/展开、任务状态切换、标签、备注、链接、附件状态展示。
- `Enter` 新建同级、`Tab` 缩进、`Shift + Tab` 减少缩进、`Option + ↑/↓` 调整同级顺序。
- 大纲模式保留 `Command/Ctrl + F` 当前导图与跨图搜索。
- 切回导图后，结构、顺序、标题、任务状态和折叠状态立即同步。

## 边界

- 不修改 React Flow 树形布局、节点卡片或连接线样式。
- 大纲不是第二份数据，不增加文档 schema 字段。
- 第一版不提供富文本排版；备注和资源继续在右侧属性面板编辑。

## 验收

- 领域模型与组件共 8 项专项测试通过。
- 真实浏览器完成视图切换、折叠、改名和回到导图后的同步验证。
- 生产构建通过。

