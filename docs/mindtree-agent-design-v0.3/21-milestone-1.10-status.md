# 里程碑 1.10：通用交换与分享完成状态

更新时间：2026-07-15

## 已完成

- 导入 OPML 2.0 为新导图，保留层级、同级顺序、备注和折叠状态。
- 导出当前导图为 OPML，便于与 XMind、MindNode 等大纲工具交换。
- 导入 Markdown 标题与缩进列表；普通段落写入最近节点备注，任务框转换为待办状态。
- OPML / Markdown 导入前展示标题、节点数、最大深度和根主题，不覆盖当前导图。
- 限制单个通用导入文件为 2 MB，并对非法 XML、空大纲和无结构 Markdown 给出错误。
- 导出完整可见导图为 SVG，支持主题背景或透明背景，不受当前缩放和平移影响。
- SVG 包含完整父子线、当前可见关系线、关系标签和折叠节点数量。

## 数据边界

- `.mindtree.json` 仍是单图完整保真格式；工作区 ZIP 仍是完整恢复格式。
- OPML / Markdown 只转换第三方格式能够稳定表达的内容，不伪造专有字段兼容性。
- 通用导入始终创建新文档，不会复用第三方 ID 或覆盖当前文档。

## 验收

- OPML 与 Markdown 使用固定样本、异常样本及 OPML 往返测试。
- SVG 使用完整树、折叠树、关系线与透明背景测试。
- 客户端 41 个测试文件、157 项测试通过；服务端 19 项测试通过。
- 真实浏览器确认导入格式入口、OPML 与 SVG 导出入口，控制台无错误。

## 后续边界

PNG / PDF 需要处理位图清晰度、分页和系统字体嵌入，暂不以低质量截图冒充完整导出。当前 SVG 可直接用于浏览器、设计工具和高质量打印。
